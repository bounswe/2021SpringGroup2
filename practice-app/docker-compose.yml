version: "3"
services:
  backend:
    build: .
    ports:
      - "9000:9000"

  database:
    image: "postgres:latest"
    env_file:
      - database.env
    ports:
      - "5432:5432"
    volumes:
      - database-data:/var/lib/postgresql/data/

volumes:
  database-data:

